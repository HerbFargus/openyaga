# The Yaga engine API, as Pajama Sam 4 uses it

Generated by `player/engine_api.py`; do not edit by hand -- the rules in the next section come from `player/engine_rules.md`.

Yaga is a C++ engine driving Python game scripts. The scripts import eleven native modules, and this is everything they reference in them, with what the openyaga shim does for each. An engine that runs the game has to provide the same names with the same behaviour.

- **Refs** counts references to `module.Name` in the recovered scripts (and the number of script files). For class members it counts uses of the member's *name* anywhere in the scripts -- a name match, so common names over-count.
- **Trace** counts engine events touching the name in 3 trace log(s) from real play: creations, calls and attribute writes the shim logs. Blank means the shim does not log it, not that it is unused.
- A member the scripts never name is left out: it is the shim's own machinery, not engine API.

## Rules an engine has to follow

Behaviour the names alone do not tell you. Each was found by the game going
wrong without it; the shim file that implements it has the details.

### The interpreter

- **`True` and `False` are 1 and 0.** The game's `boot.py` sets
  `__builtin__.False = 0` and `True = 1` for its Python 2.2 code, and that
  holds for any engine code running in the same interpreter. Never test
  `x is False`: it never matches (`yagasprite.py`, `display.py`).
- **Integer division.** The scripts are Python 2 and rely on it
  (`c_FrameRateMod = 3 / 2` is 1). Running them under Python 3 changes
  behaviour silently.
- **Python 2 orders unlike objects by address.** The sprite manager sorts
  `(z, sprite)` pairs, so equal-depth sprites are ordered by comparing the
  sprites. The shim compares engine objects by creation order, which is
  stable and usually matches the original's allocation order
  (`yagasprite.ISprite.__cmp__`).

### The loop

- One timer drives everything: `main.py` registers a receiver for
  `TIMER_TICK`, and each tick dispatches input, then ticks sound, sprites,
  rooms, scenes and preloading, in that order.
- **Input is queued, not handled, on arrival.** The game's input manager's
  `Raise` only appends to a queue; `DispatchInput` hands it out at the start of
  the next tick.
- An exception anywhere in a tick makes `main.py` call `StopEventLoop()`:
  a crash and a normal quit look the same unless the engine checks
  (`yagaevents.EventManager.StopEventLoop`).

### Sprites and animation

- **Sprites do not animate until `Run()`**, and the game calls `Run()` every
  frame, so it must be idempotent -- restarting the clock pins every
  animation on frame 0.
- **`SCENE_STOP` is load-bearing.** When an animation with a loop count
  finishes, the sprite's event sinks get `SCENE_STOP`; room changes, walking
  and cutscenes advance on it. A one-frame pose still finishes.
  Characters guard against their own `Stop()` with `ignoreCallback`, which
  only works if the stop is delivered synchronously.
- **`SCENE_RUN` repeats.** It is sent when an animation starts *and every
  time a looping animation starts over*. Scripts poll on it: a character
  looping a talking animation checks `doneTalking` on each `SCENE_RUN` and
  moves on when the line has ended, and Pajama Sam 4's inventory preview
  closes after six of them. Sent only once, Putt-Putt's first conversation
  waits for ever (`yagasprite.ISprite._advance`).
- **`position.z` is the draw order**, and a sprite's position is a value:
  assigning a Point copies it (`yagascene.copy_point`).
- **`renderRect` is known before the first draw**: the save screen centres
  labels on it the moment a sprite is made (`yagasprite._estimate_rect`).

### Layers

A frame is a stack of named layers, each with a 32-bit phoneme mask.

- A layer draws when `mask & renderMask`, or when `mask == 0` -- **except**:
  - **named alternatives**: `SetLayerFlag(name, LF_LAYER_ON, on)` switches a
    layer by name (eye directions, the bishop's crook). The switch belongs to
    the sprite and survives animation changes; layers default to on;
  - **blinks**: `BLINK1` and `BLINK2` are mask 0 and no script touches them.
    The engine blinks characters itself; the rate is not in the data;
  - **lipsync silence**: a mask of 0 in a lipsync stream means "no phoneme"
    and draws the rest pose (`ROOT`), not nothing.
- See `FORMATS.md`, "Lipsync: the phoneme mask".

### Event streams (`.evb`)

- An animation can carry an event stream: sounds to start (type 15500) and
  lipsync (type 31415). Receivers get one `Raise` per stream event, in order,
  with `deviceID` equal to the playback's `identity`; `CAnimReciever` counts
  them itself, so a skipped or repeated event reads the wrong data from then
  on (`yagaevents.EventManager._pump_streams`).
- A finished stream stops driving the mouth.
- The game asks for `.evt`; the shipped files are `.evb` (`resources.py`).

### Input

- **Mouse**: `IEVENT_AXIS_POS_X` and `IEVENT_AXIS_POS_Y` as two events
  carrying the coordinate in `value`; buttons as `IEVENT_BUTTON_DOWN`/`UP`
  with the button index in `elementID`. Coordinates are the game's 640x480.
- **Keyboard**, raw and translated: raw `BUTTON_DOWN`/`UP` carry a virtual-key
  code (letters upper case), and a translated `BUTTON_PRESS` on the way down
  carries the character typed. `KeyCodes` for non-characters start at `0x100`,
  numbered as linyaga numbers them (`yagaevents.KeyCodes`).
- Escape and Enter belong to the game: Escape opens the options menu, Enter
  skips a cutscene.

### Text

- A text sprite's position is an **anchor**: left text starts at it, centred
  text is centred on it, right text ends at it (`yagafont.py`).
- XML strings are byte strings, not unicode: they end up pickled into saves
  (`yagaxml.py`, `returns_unicode = 0`).

### Sound

- Music, dialogue and effects mix; music plays under everything at its own
  volume (0.3 by default).
- **`isPlaying` means this sound, not its channel.** A finished sound's
  channel is reused at once, and reporting the channel's state kept finished
  dialogue "playing" for ever (`yagasound.ISound.isPlaying`).
- Talkie durations time the subtitles.

### Cursor, movies, saves

- The game always takes the hardware-cursor path: `LoadCursorFile(path, id)`
  with a `.cur`, then `SetCursorByID(id)`; `cursorVisible` hides the pointer
  for cutscenes (`yagagraphics.RenderTarget`).
- Movies (`.da2`, Bink) run for exactly as long as their header says;
  `isPlaying` is timed by that, whether or not frames keep up
  (`yagasprite.IVideoElement`).
- To photograph a room for a save the game points the camera at an image
  buffer and renders again; sprites must draw wherever the camera points
  (`yagagraphics.target_surface`).
- Saves are pickles (`.dat`) plus a thumbnail (`.img`: `[w, h, [BGRA]]`),
  interchangeable with the original game's.

### Checking an implementation

Record a session with `run_game.py --record`, replay it here and in the other
engine, and compare the traces with `compare_traces.py`: a replay makes the
same engine calls as its recording, so the first difference is the first
place the two engines disagree. See `player/README.md`.

## Modules

### `yaga`

Stub for the native yaga module.  See _stub.py for how this works.

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `System` (5)

### `yagaevents`

Real stand-in for the native yagaevents module -- the engine's main loop.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `EEventClass` | class | 28 (12 files) |  |  |
| `KeyCodes` | class | 27 (8 files) |  | The engine's names for the keys that are not characters. |
| `EInputEvent` | class | 22 (11 files) |  |  |
| `IEventReciever` | class | 8 (3 files) | 258933 | Base class for anything that wants events; the game subclasses it. |
| `ERecieverReturn` | class | 5 (3 files) |  |  |
| `ETimerEvent` | class | 2 (1 files) |  |  |
| `EventManager` | function | 2 (2 files) | 46476 | The engine's event manager is a singleton. |
| `IEventStream` | class | 2 (1 files) | 13957 | A parsed .evb.  The engine builds one from a loaded resource: |
| `IEventSource` | function | 1 (1 files) |  |  |

<details><summary><code>yagaevents.EEventClass</code> -- 5 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `CLASS_GAMEPAD` | constant | 3 |  | value unknown; the game only compares against it |
| `CLASS_KEYBOARD` | constant | 12 |  | value unknown; the game only compares against it |
| `CLASS_MOUSE` | constant | 8 |  | value unknown; the game only compares against it |
| `CLASS_RENDER_TARGET` | constant | 3 |  | value unknown; the game only compares against it |
| `CLASS_TIMER` | attribute | 2 |  |  |

</details>

<details><summary><code>yagaevents.KeyCodes</code> -- 15 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `KEY_BACKSPACE` | attribute | 3 |  |  |
| `KEY_CODES_BEGIN` | attribute | 1 |  |  |
| `KEY_CONTROL` | attribute | 3 |  |  |
| `KEY_DOWN` | attribute | 1 |  |  |
| `KEY_ENTER` | attribute | 8 |  |  |
| `KEY_ESCAPE` | attribute | 2 |  |  |
| `KEY_F2` | attribute | 1 |  |  |
| `KEY_F6` | attribute | 1 |  |  |
| `KEY_F7` | attribute | 1 |  |  |
| `KEY_F8` | attribute | 1 |  |  |
| `KEY_F9` | attribute | 1 |  |  |
| `KEY_LEFT` | attribute | 1 |  |  |
| `KEY_RIGHT` | attribute | 1 |  |  |
| `KEY_SHIFT` | attribute | 1 |  |  |
| `KEY_UP` | attribute | 1 |  |  |

</details>

<details><summary><code>yagaevents.EInputEvent</code> -- 5 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `IEVENT_AXIS_POS_X` | constant | 3 |  | value unknown; the game only compares against it |
| `IEVENT_AXIS_POS_Y` | constant | 4 |  | value unknown; the game only compares against it |
| `IEVENT_BUTTON_DOWN` | constant | 8 |  | value unknown; the game only compares against it |
| `IEVENT_BUTTON_PRESS` | constant | 3 |  | value unknown; the game only compares against it |
| `IEVENT_BUTTON_UP` | constant | 4 |  | value unknown; the game only compares against it |

</details>

<details><summary><code>yagaevents.IEventReciever</code> -- 12 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `ENTER_INVENTORY` | attribute | 3 | 3 |  |
| `EXIT_INVENTORY` | attribute | 3 | 3 |  |
| `__init__` | constant | 336 |  | <unbound method IEventReciever.__init__> |
| `data` | attribute | 60 | 15503 |  |
| `idevGamepad1` | attribute | 3 | 3 |  |
| `idevGamepad2` | attribute | 3 | 3 |  |
| `idevKeyboard` | attribute | 4 | 3 |  |
| `idevMouse` | attribute | 3 | 3 |  |
| `idx` | attribute | 5 | 28717 |  |
| `inventoryVisible` | attribute | 8 | 3 |  |
| `name` | attribute | 204 | 15503 |  |
| `streamPlayback` | attribute | 20 | 29264 |  |

</details>

<details><summary><code>yagaevents.ERecieverReturn</code> -- 2 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `EVENT_HANDLED` | attribute | 4 |  |  |
| `EVENT_NOT_HANDLED` | attribute | 1 |  |  |

</details>

<details><summary><code>yagaevents.ETimerEvent</code> -- 1 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `TIMER_TICK` | attribute | 2 |  |  |

</details>

<details><summary><code>yagaevents.IEventStream</code> -- 1 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `EventData` | method | 2 |  | The groups on event `index`. |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `ERenderTargetEvent` (4), `EventSourceFlags` (2), `EvbHandler` (1), `EvtHandler` (1)

### `yagagraphics`

Partly-real stand-in for the native yagagraphics module.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `Rect` | class | 34 (14 files) |  | What sprite.renderRect is: utility.OverSprite reads .x/.y/.width/.height. |
| `PixelFormat` | class | 26 (4 files) |  | Pixel formats.  Compared and used as dictionary keys -- utility.ScaleImage looks bytes-per-pixel up by format -- so they must be stable values; as stubs they were whatever object the last access... |
| `GraphicsSystem` | function | 8 (6 files) | 77 | Singleton, as the engine's is -- the game calls this constantly. |
| `IRenderTarget` | class | 4 (2 files) | 3 | Base class the game subclasses (see print_manager.CImageBufferTarget). |
| `Color` | class | 3 (3 files) |  |  |
| `IImageAnim` | class | 3 (2 files) | 255391 | A decoded animation, as the sprite manager wraps it: |
| `ClearFlags` | class | 2 (2 files) |  | What RenderBegin is asked to clear.  Only ever tested as a bit mask -- screen_capture does `if clearFlags & ClearFlags.CLEAR_BACK` -- so it has to be a real number, and a stub made opening the... |
| `IImage` | class | 2 (1 files) |  | An engine image: IImage, and what GraphicsSystem().CreateImage makes. |

<details><summary><code>yagagraphics.PixelFormat</code> -- 12 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `PXL_A1R5G5B5` | attribute | 1 |  |  |
| `PXL_A8` | attribute | 1 |  |  |
| `PXL_A8LUM8` | attribute | 1 |  |  |
| `PXL_A8PAL8` | attribute | 1 |  |  |
| `PXL_A8R8G8B8` | attribute | 11 |  |  |
| `PXL_APAL8` | attribute | 3 |  |  |
| `PXL_LUM8` | attribute | 1 |  |  |
| `PXL_PAL8` | attribute | 3 |  |  |
| `PXL_R5G6B5` | attribute | 1 |  |  |
| `PXL_R8G8B8` | attribute | 1 |  |  |
| `PXL_X1R5G5B5` | attribute | 1 |  |  |
| `PXL_X8R8G8B8` | attribute | 1 |  |  |

</details>

<details><summary><code>yagagraphics.IRenderTarget</code> -- 1 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `__init__` | constant | 336 |  | <unbound method IRenderTarget.__init__> |

</details>

<details><summary><code>yagagraphics.IImageAnim</code> -- 8 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `Compress` | method | 1 |  |  |
| `IsAnimDone` | method | 5 |  |  |
| `anim` | attribute | 36 | 29986 |  |
| `frames` | attribute | 4 | 29986 |  |
| `framesPerSecond` | attribute | 1 | 45489 |  |
| `height` | attribute | 44 | 29986 |  |
| `locator` | attribute | 1 | 29986 |  |
| `width` | attribute | 51 | 29986 |  |

</details>

<details><summary><code>yagagraphics.ClearFlags</code> -- 1 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `CLEAR_BACK` | attribute | 2 |  |  |

</details>

<details><summary><code>yagagraphics.IImage</code> -- 7 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `Composite` | method | 3 |  |  |
| `Copy` | method | 1 |  |  |
| `Fill` | method | 2 |  |  |
| `height` | property | 44 |  |  |
| `imgData` | property | 8 |  |  |
| `pixelFmt` | property | 12 |  |  |
| `width` | property | 51 |  |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `TargetType` (9), `GeomFlags` (2), `GeomType` (2), `RenderHint` (2), `CompressionType` (1), `MngHandler` (1), `PngHandler` (1), `RleHandler` (1), `TargaHandler` (1)

### `yagasprite`

Stand-in for the native yagasprite module.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `ISprite` | class | 2 (1 files) | 1098 | Base class; the game subclasses this for its own sprite types. |
| `IVideoElement` | class | 1 (1 files) | 74 | A Bink movie -- the .da2 files, which are BIK video. |
| `Sprite` | class | 1 (1 files) | 792314 |  |
| `TalkieSprite` | class | 1 (1 files) | 5044925 |  |

<details><summary><code>yagasprite.ISprite</code> -- 25 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `AddChild` | method | 1 |  | Children are how the engine attaches a talkie's event stream. |
| `Intersect` | method | 2 |  | Per-pixel hit test, which is what the game expects. |
| `RegisterEventSink` | method | 1 |  |  |
| `RemoveChild` | method | 1 |  |  |
| `Render` | method | 7 |  | Blit the current frame's layers onto the render target. |
| `Run` | method | 17 |  | Start playing.  Sprites do not animate until asked. |
| `SetLayerFlag` | method | 6 |  | Turn a named layer on or off. |
| `SetVolume` | method | 3 |  |  |
| `Stop` | method | 20 |  |  |
| `UnregisterEventSink` | method | 1 |  |  |
| `__init__` | constant | 336 |  | <unbound method ISprite.__init__> |
| `anim` | attribute | 36 | 22 |  |
| `currentFrame` | attribute | 74 | 44 |  |
| `duration` | attribute | 6 | 22 |  |
| `frameCount` | attribute | 27 | 44 |  |
| `hFlip` | attribute | 3 | 22 |  |
| `image` | attribute | 26 | 22 |  |
| `loopCount` | attribute | 2 | 22 |  |
| `opacity` | attribute | 26 | 22 |  |
| `position` | attribute | 136 | 98 |  |
| `renderMask` | attribute | 1 | 22 |  |
| `renderRect` | attribute | 23 | 22 |  |
| `talkies` | attribute | 15 | 44 |  |
| `time` | attribute | 9 | 22 |  |
| `vFlip` | attribute | 3 | 22 |  |

</details>

<details><summary><code>yagasprite.IVideoElement</code> -- 4 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `Render` | method | 7 |  | Draw whichever frame is due. |
| `Run` | method | 17 | 37 |  |
| `Stop` | method | 20 |  |  |
| `isPlaying` | property | 9 |  |  |

</details>

<details><summary><code>yagasprite.Sprite</code> -- 14 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `Seek` | method (stub) | 2 | 142409 |  |
| `anim` | attribute | 36 | 3608 |  |
| `currentFrame` | attribute | 74 | 6484 |  |
| `duration` | attribute | 6 | 1792 |  |
| `frameCount` | attribute | 27 | 5400 |  |
| `hFlip` | attribute | 3 | 1792 |  |
| `loopCount` | attribute | 2 | 1792 |  |
| `opacity` | attribute | 26 | 1883 |  |
| `position` | attribute | 136 | 91154 |  |
| `renderMask` | attribute | 1 | 1792 |  |
| `renderRect` | attribute | 23 | 234076 |  |
| `talkies` | attribute | 15 | 3584 |  |
| `time` | attribute | 9 | 1792 |  |
| `vFlip` | attribute | 3 | 1792 |  |

</details>

<details><summary><code>yagasprite.TalkieSprite</code> -- 14 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `Seek` | method (stub) | 2 | 192349 |  |
| `anim` | attribute | 36 | 40801 |  |
| `currentFrame` | attribute | 74 | 1286719 |  |
| `duration` | attribute | 6 | 1230415 |  |
| `frameCount` | attribute | 27 | 53450 |  |
| `hFlip` | attribute | 3 | 12649 |  |
| `loopCount` | attribute | 2 | 28152 |  |
| `opacity` | attribute | 26 | 12649 |  |
| `position` | attribute | 136 | 25974 |  |
| `renderMask` | attribute | 1 | 19504 |  |
| `renderRect` | attribute | 23 | 239455 |  |
| `talkies` | attribute | 15 | 50596 |  |
| `time` | attribute | 9 | 1238990 |  |
| `vFlip` | attribute | 3 | 12649 |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `LayerFlags` (6), `BinkHandler` (1)

### `yagascene`

Stand-in for the native yagascene module.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `Point` | class | 121 (28 files) |  |  |
| `SceneEvents` | class | 120 (35 files) |  | What a sprite reports as it plays.  SCENE_STOP is load-bearing: a character's exit animation ends, the sink fires, and samCharacter then calls QueueNextScene -- which is how the game changes rooms. |
| `SceneManager` | function | 8 (6 files) |  |  |
| `ISceneEventSink` | class | 2 (1 files) | 12649 | Base class for the game's sprite callbacks; see character.CSpriteCallback, whose Event(eventType, sprite) forwards to the character. |

<details><summary><code>yagascene.Point</code> -- 5 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `__init__` | constant | 336 |  | <unbound method Point.__init__> |
| `__setattr__` | constant | 1 |  | <slot wrapper '__setattr__' of 'object' objects> |
| `x` | attribute | 216 |  |  |
| `y` | attribute | 213 |  |  |
| `z` | attribute | 76 |  |  |

</details>

<details><summary><code>yagascene.SceneEvents</code> -- 3 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `SCENE_ADVANCE` | constant | 5 |  | value unknown; the game only compares against it |
| `SCENE_RUN` | attribute | 30 |  |  |
| `SCENE_STOP` | attribute | 85 |  |  |

</details>

<details><summary><code>yagascene.ISceneEventSink</code> -- 1 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `__init__` | constant | 336 |  | <unbound method ISceneEventSink.__init__> |

</details>

### `yagasound`

Real stand-in for the native yagasound module.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `ISound` | class | 1 (1 files) | 1061 |  |
| `SoundSystem` | function | 1 (1 files) | 3 |  |

<details><summary><code>yagasound.ISound</code> -- 5 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `Run` | method | 17 | 1017 |  |
| `Stop` | method | 20 |  |  |
| `duration` | property | 6 |  | Seconds.  The script reads this to time its subtitles. |
| `isPlaying` | property | 9 |  |  |
| `position` | attribute | 136 | 44 |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `SoundSystemFlags` (1)

### `yagares`

Real stand-in for the native yagares module -- the engine's resource layer.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `ResourceManager` | class | 1 (1 files) | 4636 |  |

<details><summary><code>yagares.ResourceManager</code> -- 7 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `FlushPreloadQueue` | method | 1 |  |  |
| `Load` | method | 13 | 4612 |  |
| `LoadStream` | method | 2 |  |  |
| `Preload` | method | 6 |  |  |
| `RegisterFormatHandler` | method | 7 | 21 |  |
| `Save` | method | 4 |  |  |
| `UnregisterFormatHandler` | method | 1 |  |  |

</details>

### `yagaxml`

Real stand-in for the native yagaxml module.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `IContentHandler` | class | 16 (7 files) |  | Base class for the game's twelve XML loaders. |
| `XmlSystem` | function | 4 (4 files) |  |  |
| `Parser` | class | 3 (2 files) | 622 |  |

<details><summary><code>yagaxml.IContentHandler</code> -- 2 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `__del__` | constant | 51 |  | value unknown; the game only compares against it |
| `__init__` | constant | 336 |  | <unbound method IContentHandler.__init__> |

</details>

<details><summary><code>yagaxml.Parser</code> -- 1 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `ParseFile` | method | 7 | 622 | Parse a game path.  Returns 1 on success, 0 on failure. |

</details>

### `yagafont`

Bitmap fonts, and the subtitles they draw.

| Name | Kind | Refs | Trace | What it is |
|---|---|---|---|---|
| `FontManager` | function | 25 (8 files) |  |  |
| `HorizontalJustification` | class | 23 (8 files) |  |  |
| `VerticalJustification` | class | 21 (8 files) |  |  |

<details><summary><code>yagafont.HorizontalJustification</code> -- 3 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `HJUSTIFY_CENTER` | attribute | 7 |  |  |
| `HJUSTIFY_LEFT` | attribute | 11 |  |  |
| `HJUSTIFY_RIGHT` | attribute | 4 |  |  |

</details>

<details><summary><code>yagafont.VerticalJustification</code> -- 3 members the scripts use</summary>

| Member | Kind | Refs (by name) | Trace | What it does |
|---|---|---|---|---|
| `VJUSTIFY_BOTTOM` | attribute | 2 |  |  |
| `VJUSTIFY_CENTER` | attribute | 6 |  |  |
| `VJUSTIFY_TOP` | attribute | 12 |  |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `AsciiData` (1), `ImageStringFlags` (1)

### `yagainput`

Stub for the native yagainput module.  See _stub.py for how this works.

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `InputEvent` (1)

### `yagacollisionlib`

Stub for the native yagacollisionlib module.  See _stub.py for how this works.

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and Pajama Sam 4 plays to the end regardless. Mostly flag enums and format-handler registrations: `RinkCornerOrientation_PY` (4), `CollisionSystem` (1)

---

34 names across 11 modules; 22 more referenced names left to stubs.
