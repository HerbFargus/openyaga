# The Yaga engine API, as Pajama Sam 4 and Putt-Putt PBS use it

Generated by `player/engine_api.py`; do not edit by hand -- the rules in the next section come from `player/engine_rules.md`.

Yaga is a C++ engine driving Python game scripts. The scripts import eleven native modules, and this is everything the games reference in them, with what the openyaga shim does for each. An engine that runs the games has to provide the same names with the same behaviour.

- **Pajama Sam 4 / Putt-Putt PBS** count references to `module.Name` in each game's recovered scripts (and the number of script files); a blank means that game never names it. For class members they count uses of the member's *name* anywhere in the scripts -- a name match, so common names over-count.
- **Trace** counts engine events touching the name in trace logs from real play (3 from Pajama Sam 4, 9 from Putt-Putt PBS): creations, calls and attribute writes the shim logs. Blank means the shim does not log it, not that it is unused.
- A member no script names is left out: it is the shim's own machinery, not engine API.

## Rules an engine has to follow

Behaviour the names alone do not tell you. Each was found by the game going
wrong without it; the shim file that implements it has the details.

### The interpreter

- **`True` and `False` are 1 and 0.** The game's `boot.py` sets
  `__builtin__.False = 0` and `True = 1` for its Python 2.2 code, and that
  holds for any engine code running in the same interpreter. Never test
  `x is False`: it never matches (`yagasprite.py`, `display.py`).
- **Integer division.** The scripts are Python 2 and rely on it
  (`c_FrameRateMod = 3 / 2` is 1). Running them under Python 3 changes
  behaviour silently.
- **Python 2 orders unlike objects by address.** The sprite manager sorts
  `(z, sprite)` pairs, so equal-depth sprites are ordered by comparing the
  sprites. The shim compares engine objects by creation order, which is
  stable and usually matches the original's allocation order
  (`yagasprite.ISprite.__cmp__`).

### The loop

- One timer drives everything: `main.py` registers a receiver for
  `TIMER_TICK`, and each tick dispatches input, then ticks sound, sprites,
  rooms, scenes and preloading, in that order.
- **Input is queued, not handled, on arrival.** The game's input manager's
  `Raise` only appends to a queue; `DispatchInput` hands it out at the start of
  the next tick.
- An exception anywhere in a tick makes `main.py` call `StopEventLoop()`:
  a crash and a normal quit look the same unless the engine checks
  (`yagaevents.EventManager.StopEventLoop`).

### Sprites and animation

- **Sprites do not animate until `Run()`**, and `Run()` must be idempotent:
  the game can call it again on a sprite that is already playing, and
  restarting the clock there pins the animation on frame 0. (The call made
  every frame is the sprite manager's `Seek(delta)`, on every sprite.)
- **`SCENE_STOP` is load-bearing.** When an animation with a loop count
  finishes, the sprite's event sinks get `SCENE_STOP`; room changes, walking
  and cutscenes advance on it. A one-frame pose still finishes.
  Characters guard against their own `Stop()` with `ignoreCallback`, which
  only works if the stop is delivered synchronously.
- **`SCENE_RUN` repeats.** It is sent when an animation starts *and every
  time a looping animation starts over*. Scripts poll on it: a character
  looping a talking animation checks `doneTalking` on each `SCENE_RUN` and
  moves on when the line has ended, and Pajama Sam 4's inventory preview
  closes after six of them. Sent only once, Putt-Putt's first conversation
  waits for ever (`yagasprite.ISprite._advance`).
- **`position.z` is the draw order**, and a sprite's position is a value:
  assigning a Point copies it, and so does *reading* one. Scripts read a
  position, adjust the copy and hand it on; a live reference moves the
  original sprite instead -- in Putt-Putt, a save slot onto the slot below,
  which then took its clicks (`yagascene.copy_point`, `ISprite.__getattr__`).
- **`currentFrame` stays within the animation.** Scripts that animate by
  hand step past the end and test for the last frame exactly; unclamped,
  Putt-Putt's bunnies never finished a hop (`ISprite._clamp_frame`).
- **Hit tests are against the picture, drawn or not.** `Intersect` must work
  on a sprite the game never shows: Putt-Putt's bunny maze is a hidden mask
  (`ISprite._layers_at_rest`).
- **`renderRect` is known before the first draw**: the save screen centres
  labels on it the moment a sprite is made (`yagasprite._estimate_rect`).

### Layers

A frame is a stack of named layers, each with a 32-bit phoneme mask.

- A layer draws when `mask & renderMask`, or when `mask == 0` -- **except**:
  - **named alternatives**: `SetLayerFlag(name, LF_LAYER_ON, on)` switches a
    layer by name (eye directions, the bishop's crook). The switch belongs to
    the sprite and survives animation changes; layers default to on;
  - **blinks**: `BLINK1` and `BLINK2` are mask 0 and no script touches them.
    The engine blinks characters itself, unless the sprite's `blinkEnabled`
    is false -- Putt-Putt turns it off while a character talks. The rate is
    not in the data;
  - **lipsync silence**: a mask of 0 in a lipsync stream means "no phoneme"
    and draws the rest pose (`ROOT`), not nothing.
- See `FORMATS.md`, "Lipsync: the phoneme mask".

### Event streams (`.evb`)

- An animation can carry an event stream: sounds to start (type 15500) and
  lipsync (type 31415). Receivers get one `Raise` per stream event, in order,
  with `deviceID` equal to the playback's `identity`; `CAnimReciever` counts
  them itself, so a skipped or repeated event reads the wrong data from then
  on (`yagaevents.EventManager._pump_streams`).
- A finished stream stops driving the mouth.
- The game asks for `.evt`; the shipped files are `.evb` (`resources.py`).

### Input

- **Mouse**: `IEVENT_AXIS_POS_X` and `IEVENT_AXIS_POS_Y` as two events
  carrying the coordinate in `value`; buttons as `IEVENT_BUTTON_DOWN`/`UP`
  with the button index in `elementID`. Coordinates are the game's 640x480.
- **Keyboard**, raw and translated: raw `BUTTON_DOWN`/`UP` carry a virtual-key
  code (letters upper case), and a translated `BUTTON_PRESS` on the way down
  carries the character typed. `KeyCodes` for non-characters start at `0x100`,
  numbered as linyaga numbers them (`yagaevents.KeyCodes`).
- Escape and Enter belong to the game: Escape opens the options menu, Enter
  skips a cutscene.

### Text

- A text sprite's position is an **anchor**: left text starts at it, centred
  text is centred on it, right text ends at it (`yagafont.py`).
- XML strings are byte strings, not unicode: they end up pickled into saves
  (`yagaxml.py`, `returns_unicode = 0`).

### Sound

- Music, dialogue and effects mix; music plays under everything at its own
  volume (0.3 by default).
- **`isPlaying` means this sound, not its channel.** A finished sound's
  channel is reused at once, and reporting the channel's state kept finished
  dialogue "playing" for ever (`yagasound.ISound.isPlaying`).
- Talkie durations time the subtitles.

### Cursor, movies, saves

- The game normally takes the hardware-cursor path: `LoadCursorFile(path,
  id)` with a `.cur`, then `SetCursorByID(id)`; `cursorVisible` hides the
  pointer for cutscenes (`yagagraphics.RenderTarget`).
- **Software cursors draw centre-relative.** Some screens (Putt-Putt's cake
  decorator) turn the hardware cursor off and draw the cursor sprite,
  placed at the mouse in screen pixels. Every cursor picture is authored
  with its hotspot at the canvas centre (320, 240), so cursor art is drawn
  offset by (-320, -240) (`ISprite._origin`).
- Movies (`.da2`, Bink) run for exactly as long as their header says;
  `isPlaying` is timed by that, whether or not frames keep up
  (`yagasprite.IVideoElement`).
- To photograph a room for a save the game points the camera at an image
  buffer and renders again; sprites must draw wherever the camera points
  (`yagagraphics.target_surface`).
- Saves are pickles (`.dat`) plus a thumbnail (`.img`: `[w, h, [BGRA]]`),
  interchangeable with the original game's.

### Checking an implementation

Record a session with `run_game.py --record`, replay it here and in the other
engine, and compare the traces with `compare_traces.py`: a replay makes the
same engine calls as its recording, so the first difference is the first
place the two engines disagree. See `player/README.md`.

## Modules

### `yaga`

Stub for the native yaga module.  See _stub.py for how this works.

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `System`

### `yagaevents`

Real stand-in for the native yagaevents module -- the engine's main loop.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `EEventClass` | class | 28 (12 files) | 30 (16 files) |  |  |
| `EInputEvent` | class | 22 (11 files) | 25 (14 files) |  |  |
| `KeyCodes` | class | 27 (8 files) | 19 (7 files) |  | The engine's names for the keys that are not characters. |
| `IEventReciever` | class | 8 (3 files) | 8 (3 files) | 317281 | Base class for anything that wants events; the game subclasses it. |
| `ERecieverReturn` | class | 5 (3 files) | 5 (3 files) |  |  |
| `ETimerEvent` | class | 2 (1 files) | 2 (1 files) |  |  |
| `EventManager` | function | 2 (2 files) | 2 (2 files) | 81855 | The engine's event manager is a singleton. |
| `IEventStream` | class | 2 (1 files) | 2 (1 files) | 17422 | A parsed .evb.  The engine builds one from a loaded resource: |
| `IEventSource` | function | 1 (1 files) | 1 (1 files) |  |  |

<details><summary><code>yagaevents.EEventClass</code> -- 5 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `CLASS_GAMEPAD` | constant | 3 | 3 |  | value unknown; the games only compare against it |
| `CLASS_KEYBOARD` | constant | 12 | 10 |  | value unknown; the games only compare against it |
| `CLASS_MOUSE` | constant | 8 | 12 |  | value unknown; the games only compare against it |
| `CLASS_RENDER_TARGET` | constant | 3 | 3 |  | value unknown; the games only compare against it |
| `CLASS_TIMER` | attribute | 2 | 2 |  |  |

</details>

<details><summary><code>yagaevents.EInputEvent</code> -- 5 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `IEVENT_AXIS_POS_X` | constant | 3 | 3 |  | value unknown; the games only compare against it |
| `IEVENT_AXIS_POS_Y` | constant | 4 | 3 |  | value unknown; the games only compare against it |
| `IEVENT_BUTTON_DOWN` | constant | 8 | 12 |  | value unknown; the games only compare against it |
| `IEVENT_BUTTON_PRESS` | constant | 3 | 2 |  | value unknown; the games only compare against it |
| `IEVENT_BUTTON_UP` | constant | 4 | 5 |  | value unknown; the games only compare against it |

</details>

<details><summary><code>yagaevents.KeyCodes</code> -- 15 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `KEY_BACKSPACE` | attribute | 3 | 2 |  |  |
| `KEY_CODES_BEGIN` | attribute | 1 | 1 |  |  |
| `KEY_CONTROL` | attribute | 3 | 3 |  |  |
| `KEY_DOWN` | attribute | 1 | 1 |  |  |
| `KEY_ENTER` | attribute | 8 | 5 |  |  |
| `KEY_ESCAPE` | attribute | 2 | 3 |  |  |
| `KEY_F2` | attribute | 1 |  |  |  |
| `KEY_F6` | attribute | 1 |  |  |  |
| `KEY_F7` | attribute | 1 |  |  |  |
| `KEY_F8` | attribute | 1 |  |  |  |
| `KEY_F9` | attribute | 1 |  |  |  |
| `KEY_LEFT` | attribute | 1 | 1 |  |  |
| `KEY_RIGHT` | attribute | 1 | 1 |  |  |
| `KEY_SHIFT` | attribute | 1 | 1 |  |  |
| `KEY_UP` | attribute | 1 | 1 |  |  |

</details>

<details><summary><code>yagaevents.IEventReciever</code> -- 12 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `ENTER_INVENTORY` | attribute | 3 |  | 3 |  |
| `EXIT_INVENTORY` | attribute | 3 |  | 3 |  |
| `__init__` | constant | 336 | 256 |  | <unbound method IEventReciever.__init__> |
| `data` | attribute | 60 | 84 | 20714 |  |
| `idevGamepad1` | attribute | 3 | 3 | 12 |  |
| `idevGamepad2` | attribute | 3 | 3 | 12 |  |
| `idevKeyboard` | attribute | 4 | 4 | 12 |  |
| `idevMouse` | attribute | 3 | 4 | 12 |  |
| `idx` | attribute | 5 | 5 | 36106 |  |
| `inventoryVisible` | attribute | 8 |  | 3 |  |
| `name` | attribute | 204 | 222 | 20714 |  |
| `streamPlayback` | attribute | 20 | 20 | 37372 |  |

</details>

<details><summary><code>yagaevents.ERecieverReturn</code> -- 2 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `EVENT_HANDLED` | attribute | 4 | 4 |  |  |
| `EVENT_NOT_HANDLED` | attribute | 1 | 1 |  |  |

</details>

<details><summary><code>yagaevents.ETimerEvent</code> -- 1 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `TIMER_TICK` | attribute | 2 | 2 |  |  |

</details>

<details><summary><code>yagaevents.IEventStream</code> -- 1 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `EventData` | method | 2 | 2 |  | The groups on event `index`. |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `ERenderTargetEvent`, `EventSourceFlags`, `EvbHandler`, `EvtHandler`, `IMouseInputDevice` (Putt-Putt PBS only)

### `yagagraphics`

Partly-real stand-in for the native yagagraphics module.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `Rect` | class | 34 (14 files) | 40 (13 files) |  | What sprite.renderRect is: utility.OverSprite reads .x/.y/.width/.height. |
| `PixelFormat` | class | 26 (4 files) | 29 (7 files) |  | Pixel formats.  Compared and used as dictionary keys -- utility.ScaleImage looks bytes-per-pixel up by format -- so they must be stable values; as stubs they were whatever object the last access... |
| `GraphicsSystem` | function | 8 (6 files) | 10 (9 files) | 252 | Singleton, as the engine's is -- the game calls this constantly. |
| `Color` | class | 3 (3 files) | 7 (6 files) |  |  |
| `IRenderTarget` | class | 4 (2 files) | 4 (2 files) | 8 | Base class the game subclasses (see print_manager.CImageBufferTarget). |
| `IImageAnim` | class | 3 (2 files) | 3 (2 files) | 373114 | A decoded animation, as the sprite manager wraps it: |
| `IImage` | class | 2 (1 files) | 3 (2 files) |  | An engine image: IImage, and what GraphicsSystem().CreateImage makes. |
| `ClearFlags` | class | 2 (2 files) | 2 (2 files) |  | What RenderBegin is asked to clear.  Only ever tested as a bit mask -- screen_capture does `if clearFlags & ClearFlags.CLEAR_BACK` -- so it has to be a real number, and a stub made opening the... |

<details><summary><code>yagagraphics.PixelFormat</code> -- 12 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `PXL_A1R5G5B5` | attribute | 1 | 1 |  |  |
| `PXL_A8` | attribute | 1 | 1 |  |  |
| `PXL_A8LUM8` | attribute | 1 | 1 |  |  |
| `PXL_A8PAL8` | attribute | 1 | 1 |  |  |
| `PXL_A8R8G8B8` | attribute | 11 | 14 |  |  |
| `PXL_APAL8` | attribute | 3 | 3 |  |  |
| `PXL_LUM8` | attribute | 1 | 1 |  |  |
| `PXL_PAL8` | attribute | 3 | 3 |  |  |
| `PXL_R5G6B5` | attribute | 1 | 1 |  |  |
| `PXL_R8G8B8` | attribute | 1 | 1 |  |  |
| `PXL_X1R5G5B5` | attribute | 1 | 1 |  |  |
| `PXL_X8R8G8B8` | attribute | 1 | 1 |  |  |

</details>

<details><summary><code>yagagraphics.IRenderTarget</code> -- 1 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `__init__` | constant | 336 | 256 |  | <unbound method IRenderTarget.__init__> |

</details>

<details><summary><code>yagagraphics.IImageAnim</code> -- 9 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `Compress` | method | 1 | 1 |  |  |
| `IsAnimDone` | method | 5 |  |  |  |
| `anim` | attribute | 36 | 70 | 43371 |  |
| `frames` | attribute | 4 | 14 | 43371 |  |
| `framesPerSecond` | attribute | 1 | 1 | 64085 |  |
| `height` | attribute | 44 | 53 | 43371 |  |
| `locator` | attribute | 1 | 1 | 43371 |  |
| `palette` | method (stub) | 4 | 15 | 5432 |  |
| `width` | attribute | 51 | 56 | 43371 |  |

</details>

<details><summary><code>yagagraphics.IImage</code> -- 7 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `Composite` | method | 3 | 9 |  |  |
| `Copy` | method | 1 | 1 |  |  |
| `Fill` | method | 2 | 4 |  |  |
| `height` | property | 44 | 53 |  |  |
| `imgData` | property | 8 | 8 |  |  |
| `pixelFmt` | property | 12 | 7 |  |  |
| `width` | property | 51 | 56 |  |  |

</details>

<details><summary><code>yagagraphics.ClearFlags</code> -- 1 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `CLEAR_BACK` | attribute | 2 | 2 |  |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `TargetType`, `IPalette` (Putt-Putt PBS only), `GeomFlags`, `GeomType`, `RenderHint`, `CompressionType`, `MngHandler`, `PngHandler`, `RleHandler`, `TargaHandler`

### `yagasprite`

Stand-in for the native yagasprite module.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `ISprite` | class | 2 (1 files) | 2 (1 files) | 10869 | Base class; the game subclasses this for its own sprite types. |
| `IVideoElement` | class | 1 (1 files) | 1 (1 files) | 182 | A Bink movie -- the .da2 files, which are BIK video. |
| `Sprite` | class | 1 (1 files) | 1 (1 files) | 3607488 |  |
| `TalkieSprite` | class | 1 (1 files) | 1 (1 files) | 6554077 |  |

<details><summary><code>yagasprite.ISprite</code> -- 25 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `AddChild` | method | 1 | 1 |  | Children are how the engine attaches a talkie's event stream. |
| `Intersect` | method | 2 | 2 |  | Per-pixel hit test, which is what the game expects. |
| `RegisterEventSink` | method | 1 | 1 |  |  |
| `RemoveChild` | method | 1 | 1 |  |  |
| `Render` | method | 7 | 7 |  | Blit the current frame's layers onto the render target. |
| `Run` | method | 17 | 16 |  | Start playing.  Sprites do not animate until asked. |
| `SetLayerFlag` | method | 6 | 28 |  | Turn a named layer on or off. |
| `SetVolume` | method | 3 |  |  |  |
| `Stop` | method | 20 | 24 |  |  |
| `UnregisterEventSink` | method | 1 | 1 |  |  |
| `__init__` | constant | 336 | 256 |  | <unbound method ISprite.__init__> |
| `anim` | attribute | 36 | 70 | 93 |  |
| `currentFrame` | attribute | 74 | 138 | 186 |  |
| `duration` | attribute | 6 | 6 | 93 |  |
| `frameCount` | attribute | 27 | 37 | 186 |  |
| `hFlip` | attribute | 3 | 3 | 93 |  |
| `image` | attribute | 26 | 30 | 93 |  |
| `loopCount` | attribute | 2 | 4 | 93 |  |
| `opacity` | attribute | 26 | 22 | 93 |  |
| `position` | attribute | 136 | 169 | 3528 |  |
| `renderMask` | attribute | 1 | 1 | 93 |  |
| `renderRect` | attribute | 23 | 32 | 93 |  |
| `talkies` | attribute | 15 | 15 | 186 |  |
| `time` | attribute | 9 | 13 | 93 |  |
| `vFlip` | attribute | 3 | 3 | 93 |  |

</details>

<details><summary><code>yagasprite.IVideoElement</code> -- 4 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `Render` | method | 7 | 7 |  | Draw whichever frame is due. |
| `Run` | method | 17 | 16 | 91 |  |
| `Stop` | method | 20 | 24 |  |  |
| `isPlaying` | property | 9 | 9 |  |  |

</details>

<details><summary><code>yagasprite.Sprite</code> -- 14 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `Seek` | method (stub) | 2 | 2 | 734745 |  |
| `anim` | attribute | 36 | 70 | 15118 |  |
| `currentFrame` | attribute | 74 | 138 | 39820 |  |
| `duration` | attribute | 6 | 6 | 6521 |  |
| `frameCount` | attribute | 27 | 37 | 21639 |  |
| `hFlip` | attribute | 3 | 3 | 6521 |  |
| `loopCount` | attribute | 2 | 4 | 6521 |  |
| `opacity` | attribute | 26 | 22 | 6612 |  |
| `position` | attribute | 136 | 169 | 250550 |  |
| `renderMask` | attribute | 1 | 1 | 6521 |  |
| `renderRect` | attribute | 23 | 32 | 987942 |  |
| `talkies` | attribute | 15 | 15 | 13042 |  |
| `time` | attribute | 9 | 13 | 6521 |  |
| `vFlip` | attribute | 3 | 3 | 6521 |  |

</details>

<details><summary><code>yagasprite.TalkieSprite</code> -- 15 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `Seek` | method (stub) | 2 | 2 | 323585 |  |
| `anim` | attribute | 36 | 70 | 48707 |  |
| `blinkEnabled` | attribute |  | 4 | 2048 |  |
| `currentFrame` | attribute | 74 | 138 | 1586652 |  |
| `duration` | attribute | 6 | 6 | 1517153 |  |
| `frameCount` | attribute | 27 | 37 | 62701 |  |
| `hFlip` | attribute | 3 | 3 | 13994 |  |
| `loopCount` | attribute | 2 | 4 | 34708 |  |
| `opacity` | attribute | 26 | 22 | 13994 |  |
| `position` | attribute | 136 | 169 | 29928 |  |
| `renderMask` | attribute | 1 | 1 | 34954 |  |
| `renderRect` | attribute | 23 | 32 | 380224 |  |
| `talkies` | attribute | 15 | 15 | 55976 |  |
| `time` | attribute | 9 | 13 | 1526779 |  |
| `vFlip` | attribute | 3 | 3 | 13994 |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `LayerFlags`, `BinkHandler`

### `yagascene`

Stand-in for the native yagascene module.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `Point` | class | 121 (28 files) | 107 (24 files) |  |  |
| `SceneEvents` | class | 120 (35 files) | 86 (25 files) |  | What a sprite reports as it plays.  SCENE_STOP is load-bearing: a character's exit animation ends, the sink fires, and samCharacter then calls QueueNextScene -- which is how the game changes rooms. |
| `SceneManager` | function | 8 (6 files) | 3 (3 files) |  |  |
| `ISceneEventSink` | class | 2 (1 files) | 2 (1 files) | 13994 | Base class for the game's sprite callbacks; see character.CSpriteCallback, whose Event(eventType, sprite) forwards to the character. |

<details><summary><code>yagascene.Point</code> -- 5 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `__init__` | constant | 336 | 256 |  | <unbound method Point.__init__> |
| `__setattr__` | constant | 1 | 1 |  | <slot wrapper '__setattr__' of 'object' objects> |
| `x` | attribute | 216 | 215 |  |  |
| `y` | attribute | 213 | 213 |  |  |
| `z` | attribute | 76 | 82 |  |  |

</details>

<details><summary><code>yagascene.SceneEvents</code> -- 3 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `SCENE_ADVANCE` | constant | 5 | 5 |  | value unknown; the games only compare against it |
| `SCENE_RUN` | attribute | 30 | 29 |  |  |
| `SCENE_STOP` | attribute | 85 | 52 |  |  |

</details>

<details><summary><code>yagascene.ISceneEventSink</code> -- 1 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `__init__` | constant | 336 | 256 |  | <unbound method ISceneEventSink.__init__> |

</details>

### `yagasound`

Real stand-in for the native yagasound module.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `ISound` | class | 1 (1 files) | 1 (1 files) | 3627 |  |
| `SoundSystem` | function | 1 (1 files) | 1 (1 files) | 12 |  |

<details><summary><code>yagasound.ISound</code> -- 5 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `Run` | method | 17 | 16 | 3583 |  |
| `Stop` | method | 20 | 24 |  |  |
| `duration` | property | 6 | 6 |  | Seconds.  The script reads this to time its subtitles. |
| `isPlaying` | property | 9 | 9 |  |  |
| `position` | attribute | 136 | 169 | 44 |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `SoundSystemFlags`

### `yagares`

Real stand-in for the native yagares module -- the engine's resource layer.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `ResourceManager` | class | 1 (1 files) | 1 (1 files) | 10851 |  |

<details><summary><code>yagares.ResourceManager</code> -- 7 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `FlushPreloadQueue` | method | 1 | 2 |  |  |
| `Load` | method | 13 | 13 | 10755 |  |
| `LoadStream` | method | 2 | 2 |  |  |
| `Preload` | method | 6 | 9 |  |  |
| `RegisterFormatHandler` | method | 7 | 7 | 84 |  |
| `Save` | method | 4 | 3 |  |  |
| `UnregisterFormatHandler` | method | 1 | 1 |  |  |

</details>

### `yagaxml`

Real stand-in for the native yagaxml module.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `IContentHandler` | class | 16 (7 files) | 15 (7 files) |  | Base class for the game's twelve XML loaders. |
| `XmlSystem` | function | 4 (4 files) | 4 (4 files) |  |  |
| `Parser` | class | 3 (2 files) | 3 (2 files) | 1576 |  |

<details><summary><code>yagaxml.IContentHandler</code> -- 2 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `__del__` | constant | 51 | 6 |  | value unknown; the games only compare against it |
| `__init__` | constant | 336 | 256 |  | <unbound method IContentHandler.__init__> |

</details>

<details><summary><code>yagaxml.Parser</code> -- 1 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `ParseFile` | method | 7 | 7 | 1576 | Parse a game path.  Returns 1 on success, 0 on failure. |

</details>

### `yagafont`

Bitmap fonts, and the subtitles they draw.

| Name | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it is |
|---|---|---|---|---|---|
| `FontManager` | function | 25 (8 files) | 21 (8 files) |  |  |
| `HorizontalJustification` | class | 23 (8 files) | 23 (10 files) |  |  |
| `VerticalJustification` | class | 21 (8 files) | 19 (8 files) |  |  |

<details><summary><code>yagafont.HorizontalJustification</code> -- 3 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `HJUSTIFY_CENTER` | attribute | 7 | 8 |  |  |
| `HJUSTIFY_LEFT` | attribute | 11 | 11 |  |  |
| `HJUSTIFY_RIGHT` | attribute | 4 | 3 |  |  |

</details>

<details><summary><code>yagafont.VerticalJustification</code> -- 3 members the scripts use</summary>

| Member | Kind | Pajama Sam 4 | Putt-Putt PBS | Trace | What it does |
|---|---|---|---|---|---|
| `VJUSTIFY_BOTTOM` | attribute | 2 | 2 |  |  |
| `VJUSTIFY_CENTER` | attribute | 6 | 7 |  |  |
| `VJUSTIFY_TOP` | attribute | 12 | 9 |  |  |

</details>

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `AsciiData`, `ImageStringFlags`

### `yagainput`

Stub for the native yagainput module.  See _stub.py for how this works.

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `InputEvent`

### `yagacollisionlib`

Stub for the native yagacollisionlib module.  See _stub.py for how this works.

**Left to stubs.** Referenced by the scripts but not defined in the shim, so they resolve to a tracing stub that accepts anything -- and the games play to the end regardless. Mostly flag enums and format-handler registrations: `RinkCornerOrientation_PY`, `CollisionSystem`

---

34 names across 11 modules: 34 used by both games, 0 by Pajama Sam 4 only, 0 by Putt-Putt PBS only. 24 more referenced names left to stubs.
